# Landmarks Game - 快速启动指南

## 🎮 游戏玩法

### 基本概念
1. **领队**看到完整地图，为词语写描述
2. **队员**根据描述，选择一个相邻格子，输入词语
3. 重复直到探索到**出口** ✓

### 操作步骤

#### 初始化
1. 打开 `index.html` 在浏览器中
2. 在设置界面配置：
   - 地图大小（默认 6×6）
   - 出口数量（默认 1）
3. 点击「开始游戏」

#### 每一轮

**领队阶段**
- 在文本框输入词语的描述/提示
- 点击「提交描述」→ **切换到队员**

**队员阶段**
- 看到领队的描述
- **点击地图上的一个格子**（绿色=可选）
- 在输入框输入词语（不能重复）
- 点击「赋予词汇到格子」

#### 游戏结束
- 达到出口 🚪 → **胜利** 🎉

---

## 💻 技术细节

### 格子状态
| 状态 | 颜色 | 含义 |
|------|------|------|
| 已探索 | 棕色 | 选中过，显示词语 |
| 可选 | 绿色 | 相邻于已探索，可点击 |
| 不可选 | 暗 | 远离探索区 |
| 出口 | 金色+🚪 | 到达即胜利 |

### 六边形网格
- 6×6 = 36 个格子
- 每个格子最多 6 个邻接
- 可在设置中改为其他大小（4-12）

### 词语管理
- 词语必须**全局唯一**
- 初始 3 个词来自词库
- 后续词语由队员创造

---

## 🐛 调试技巧

### 浏览器控制台
```javascript
// 查看当前游戏状态
debugState()

// 输出：
// - Phase: 游戏阶段
// - Game Mode: 领队/队员
// - Round: 轮数
// - Used Words: 已用词语
// - Full State: 完整状态对象
```

### 快捷键
- **Ctrl + Enter**：快速提交（在文本框中）
- **Escape**：清除当前选择

---

## 📦 文件结构

```
landmarks-game/
├── index.html              # 主界面
├── style.css               # 样式
├── data/
│   └── vocabularies.js     # 词库 + 配置
└── js/
    ├── vocabulary.js       # 词语工具
    ├── mapEngine.js        # 网格逻辑
    ├── gameEngine.js       # 游戏状态
    ├── controller.js       # 事件处理
    └── renderer.js         # DOM 更新
```

---

## 🎨 自定义

### 改变地图配置
在 `data/vocabularies.js` 中修改 `gameConfig`：

```javascript
const gameConfig = {
  gridSize: 8,              // 改为 8×8
  initialBlockCount: 5,     // 初始 5 个格子
  exitCount: 2              // 2 个出口
};
```

### 改变初始词库
在 `data/vocabularies.js` 中修改 `VOCABULARY_DATA`：

```javascript
{
  id: 'unique_id',
  zh: '中文名',
  en: 'English',
  hint: '在游戏中显示的提示',
  categoryId: 'category'
}
```

---

## ❓ FAQ

**Q: 词语可以重复吗？**
A: 不可以。系统会提醒"该词已被使用"。

**Q: 能改变网格大小吗？**
A: 可以。在设置界面调整，或在代码中修改配置。

**Q: 出口怎么看？**
A: 出口格子会显示 🚪 符号和金色边框。

**Q: 有分数系统吗？**
A: 当前版本没有，仅计算轮数。

**Q: 能多人玩吗？**
A: 当前设计支持单个领队和单个队员。多人支持需另行开发。

---

## 🚀 后续优化

- [ ] 六边形网格的 SVG 可视化
- [ ] 多队员轮流机制
- [ ] 计分系统（早期发现奖励）
- [ ] 游戏暂停/保存
- [ ] 音效和动画
- [ ] 移动端适配

