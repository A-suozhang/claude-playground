# AI Prompt 优化总结

## 问题
AI队员当前策略过于保守，主要只选择与**两个或以上已知词汇相邻**的格子，导致：
- ❌ 地图探索不足，词汇聚集
- ❌ 新词汇难以有好的放置位置
- ❌ 游戏进度慢，路线单调

## 解决方案
修改 `js/aiAgent.js` 的 `buildPrompt()` 函数，引入**三层相关性判断**：

### 核心改进：从"能不能相邻"到"值不值得探索"

```javascript
旧逻辑：
IF 存在高度相关词 AND 与其相邻
  THEN 选择这个格子
ELSE 不选（或被迫选弱相关）

新逻辑：
IF 存在LEVEL 1强相关词 AND 相邻
  THEN 选择(置信度0.9) ✓ 最优

IF 存在LEVEL 2中相关词 AND (相邻 OR 距离2步+几何接近)
  THEN 选择(置信度0.75-0.8) ✓ 很好

IF 只有LEVEL 3弱相关词 but 与某个词相邻+靠近多个其他词
  THEN 选择(置信度0.65) ✓ 积极探索

IF 多个弱相关选项
  THEN 选择最有"连接潜力"的 ✓ 开拓新地域
```

## 相关性分类标准

### LEVEL 1 强相关 ⭐⭐⭐
满足≥2个条件：
- 同地域 + 同类型（如：埃菲尔铁塔 + 巴黎圣母院）
- 同朝代 + 同防御体系（如：长城 + 秦陵）
- 直接历史联系（如：金字塔 + 狮身人面像）

### LEVEL 2 中相关 ⭐⭐
满足1-2个条件：
- 同大洲/地区（如：埃及金字塔 + 迦太基遗迹）
- 同类型但不同文明（如：不同地区的城堡）
- 相关时期（如：古罗马 + 秦朝）

### LEVEL 3 弱相关 ⭐
仅分类相关：
- 同形态不同文明（如：金字塔 + 玛雅金字塔）
- 地理特征（如：两条河流）
- 主题分类（如：都是"防御建筑"）

## 关键变化

| 情景 | 旧策略 | 新策略 |
|------|--------|----------|
| **与强相关词相邻** | 选择(0.9) | 选择(0.9) ✓ 相同 |
| **只与弱相关词相邻** | 被迫选择(0.5) | 聚焦探索(0.65) ✓ 更自信 |
| **与中相关词距离2步+几何近** | 避免 | 可接受(0.7) ✓ 更灵活 |
| **多个弱相关选项** | 被动选最近 | 主动找"连接潜力"最高的 ✓ 更聪明 |

## 实际游戏效果对比

### 情景1：词汇集中在一个地区
```
已有：[埃菲尔, 罗马斗兽场, 比萨斜塔]
新词：泰姬陵（完全不同地域）

旧AI：找不到相邻的、被迫选择离已知词"相对近"的格子
结果：词汇强行集中，地图扭曲

新AI：识别为LEVEL 3弱相关但主动探索
     选择与某词相邻+靠近多词的位置，开拓新地域
结果：地图扩展，为更多亚洲词汇预留空间
```

### 情景2：有中强度关联
```
已有：[长城, 故宫]
新词：秦始皇陵

旧AI：如果长城相邻区域都占满，可能选择远处
新AI：识别LEVEL 1强相关，距离2步但几何近(150px)也接受
结果：中国古迹聚集，更合理的历史布局
```

### 情景3：地图中期，开始填空
```
已有：四个大地理区域各有词汇，中心较空

旧AI：新词多为弱相关，AI犹豫，选择被迫
新AI：积极探索模式！选择中心并与外围各区相连
结果：地图生机勃勃，中心成为新的聚类核心
```

## 代码变更

**文件**：`js/aiAgent.js`
**函数**：`buildPrompt(currentWord, candidateContexts, state)`
**行数**：~80行 prompt 文本优化
**兼容性**：✅ 完全兼容（所有25个单元测试通过）

## 新 Prompt 的关键部分

```markdown
【相关性层级定义】：
- LEVEL 1: 强相关(地域+类型都匹配)
- LEVEL 2: 中相关(同地区/同类型)
- LEVEL 3: 弱相关(分类相关)

【核心决策策略】：
模式A：强相关存在 → 与其相邻(0.9置信度)
模式B：只有中相关 → 相邻或2步近距离可接受(0.75)
模式C：积极探索 → 与1词相邻+靠近多词 = 好选择(0.65)

【重点提醒】：
不要被"高度相关"绑架！
即使没有LEVEL 1词汇，也要敢于探索
只与1个词相邻但接近多个词 = 很好的探索选择
```

## 使用说明

✅ **自动生效** - 下次用户开启 AI 队员模式，新 Prompt 自动使用
✅ **无需重新部署** - 纯提示词优化，不改变游戏逻辑
✅ **测试通过** - 所有 25 个 AI 单元测试仍然全部通过

## 游戏体验改进

### 玩家将看到的变化
1. **AI 更果敢** - 不再反复选择同一区域的相邻格子
2. **地图更平衡** - 词汇自然分散，形成多个聚类
3. **游戏更快** - 探索更积极，进度加快
4. **策略更清晰** - reasoning 包含 LEVEL 分类，易于理解为什么选择

### 示例推理语句
```
"LEVEL 1强相关(同国家)，距离1步相邻 ✓"
"LEVEL 2中相关(同亚洲)，距离2步但几何150px，值得探索"
"LEVEL 3弱相关，积极探索模式，开拓新地域"
```

## 技术细节

- **Prompt 文本**: ~400 行 → ~500 行（增加了分类和例子）
- **解析逻辑**: 无改变（仍然解析 JSON 格式）
- **Fallback 机制**: 无改变（API 超时仍使用本地贪心）
- **置信度范围**: 0.55 ~ 0.9（比之前的 0.5 ~ 0.9 更精细）

## 与现有系统的关系

✅ **兼容**
- gameEngine.js 的逻辑不变
- renderer.js 的显示不变
- controller.js 的流程不变
- aiMapAnalyzer.js 的输出不变
- 仅 buildPrompt() 文本变优化

✅ **测试完整**
```
aiAgent.test.js: 25/25 通过
blessing-curse-fix.test.js: 19/19 通过
map-generation-fix.test.js: 10/10 通过
```

## 文档位置

详细说明：[AI_EXPLORATION_STRATEGY.md](./AI_EXPLORATION_STRATEGY.md)

---

**更新**: 2026-02-28
**改动**: js/aiAgent.js - buildPrompt() 函数优化
**影响**: AI 决策策略，游戏体验改进
**兼容性**: ✅ 完全向后兼容
