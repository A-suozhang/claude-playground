# AI 策略 v2 最终优化：低相关词内部梯度分析

## 问题发现

用户在使用 v2 策略后提出了一个关键的改进点：

**场景**：新词只与一个词高度相关，其他词相关度都很低
- 旧 v2 做法：选择与TOP词相邻，不考虑其他词
- **问题**：忽视了"低相关词之间也有相对高低"

**例子**：
```
新词「秦陵」与已有词：
  秦长城    → 90% (TOP)
  故宫      → 75% (次级)
  埃及金字塔 → 20% (低)
  泰姬陵    → 15% (更低)

v2 决策：选择与秦长城相邻 ✓ 正确

但如果有多个与秦长城都相邻的候选位置呢？
  位置A：与秦长城相邻(50px)，距故宫150px，距埃及200px
  位置B：与秦长城相邻(50px)，距埃及80px，距故宫250px

v2.0 可能无法区分两者
v2.1 应该优先选择位置A → 靠近故宫(75%)而不是埃及(20%)
```

---

## v2.1 的核心改进：全级递阶比较

### 原理

**之前的逻辑**：
```
找TOP词 → 靠近TOP词 → 完成
```

**改进后的逻辑**：
```
列出所有词的相关度排序
  ↓
识别梯度层级 (陡峭/平缓/平散)
  ↓
根据梯度选择靠近的词
  ↓
在该词的多个相邻位置中，优先选择同时靠近"次级词"的位置
  ↓
遵循完整的相对高低链：高→次高→中→次低→最低
```

### 三个应用场景

#### 1️⃣ 陡峭梯度中的次级词优化

**特征**：
```
TOP词(90%) >> 次级词(75%) >>> 低词(20%) > 更低词(15%)
             |____________相差15%____________|
                          |________相差55%_________|
```

**v2.1 决策**：
```
第1选：与TOP词(90%)相邻 → 是必选的基础

第2选：在与TOP词相邻的多个位置中：
  ✓ 优先选择同时靠近次级词(75%)的位置
  ✗ 避免选择同时靠近低词(20%)的位置

理由：
  - TOP词(90%)是绝对必需
  - 次级词(75%)相对较高，保留其聚集空间
  - 低词(20%)相关度太低，不必优先靠近

结果：形成两个聚类中心
  - 秦长城(90%) + 故宫(75%) 聚在一起 ← 强聚类
  - 埃及/泰姬陵(15-20%) 在远处 ← 分离保持
```

#### 2️⃣ 平缓梯度中的相等权衡

**特征**：
```
词A(85%) ≈ 词B(82%) ≈ 词C(78%) ≈ 词D(75%)
   |____________差异<10%____________|
```

**v2.1 决策**：
```
所有词都相关度接近，应该找"中心"位置
→ 如果无理想中心，则在选择某词的相邻位置时：
  ✓ 尽量同时靠近其他几个词
  ✓ 形成"多核心聚集区域"

例如与词A(85%)相邻的位置中：
  优先选择距B(82%)、C(78%)也近的位置
  而不是距离最远的位置
```

#### 3️⃣ 平散梯度中的相对高低识别 ⭐ 核心创新

**特征**：
```
所有词都低相关度，但内部有梯度：
词A(45%) > 词B(42%) >> 词C(28%) > 词D(25%)
  |________相差3%_______|
                |_______相差17%_______|
```

**v2.0 做法**：
- 选择与词A相邻（45%是最高）
- 不区分词A/B与词C/D的巨大差异

**v2.1 改进**：
```
虽然所有词都不太相关，但"相对高低"仍然重要！

分组识别：
  高相对组（内部接近）：词A(45%) > 词B(42%) — 差异小
  低相对组（内部接近）：词C(28%) > 词D(25%) — 差异小
  组间差异：(45-42) vs (28-25) 都小，但 42% vs 28% 差17个百分点！

决策原则：
  1. 必须与高相对组靠近（A或B）
  2. 在与A相邻的多个位置中，优先靠近B
  3. 完全避免靠近低相对组（C或D）

例子：
  新词「撒哈拉」: 尼罗河45% > 开罗42% >> 长城28% > 泰姬陵25%

  位置X：与尼罗河相邻，距开罗150px
  位置Y：与尼罗河相邻，距长城80px

  选位置X！
  原因：虽然距离看起来位置Y更优(80px vs 150px)
       但长城(28%)的相关度远低于开罗(42%)
       应该优先保护开罗的聚集空间
```

---

## 具体实现指南

### 步骤 1-2：相关度排序和梯度识别（不变）
（保持 v2 原有的流程）

### 步骤 3：候选位置评估（新增细节）

#### 对于陡峭梯度：

```
原有逻辑：
  与TOP词相邻 → 置信度 0.90+

改进逻辑：
  与TOP词相邻（这是基础）
    ├─ 如果有多个相邻位置：
    │   └─ 比较它们到"第二高相关词"的距离
    │       └─ 选择同时靠近次级词的位置
    │
    └─ 这样形成"TOP词+次级词"的紧密聚集

例：「秦陵」与 秦长城90%、故宫75%、埃及20%

候选位置评估：
  位置A（与秦长城相邻50px）：
    到故宫：100px  ← 相对近
    到埃及：250px  ← 很远
    综合评分：0.92 ✓ 优选

  位置B（与秦长城相邻50px）：
    到故宫：280px  ← 相对远
    到埃及：50px   ← 很近
    综合评分：0.85 ✗ 次选
```

#### 对于平散梯度：

```
原有逻辑：
  与最高相关词相邻 → 置信度 0.65+

改进逻辑：
  首先识别"相对高低分组"
    ├─ 高组：与最高相关词一起（相对接近的几个）
    │   └─ 位置应该靠近这一组的词
    │
    └─ 低组：相对低的词（即使最高的也只是"次低"）
        └─ 位置应该避免靠近这一组

例：「撒哈拉」与 尼罗河45%、开罗42%、长城28%、泰姬陵25%

分组识别：
  高相对组（内部相近）：尼罗河45% > 开罗42% (差异3%)
  低相对组（内部相近）：长城28% > 泰姬陵25% (差异3%)
  分组间差异：42% vs 28% = 14个百分点！

位置评估：
  位置X（与尼罗河相邻）：
    到开罗：150px ← 次级组内接近
    到长城：300px ← 距低组远
    综合评分：0.72 ✓ 优选

  位置Y（与尼罗河相邻）：
    到开罗：280px ← 次级组内距离远
    到长城：80px  ← 接近低组，不该这样
    综合评分：0.58 ✗ 次选
```

---

## Reasoning 的表达方式

### 旧 v2：
```
"TOP词90%远高于其他词，选与其相邻"
```

### 新 v2.1：
```
"梯度陡峭：TOP词90% >> 次级词75% >>> 低词20%，选与TOP词相邻同时靠近次级词75%"

或

"平散梯度中45%/42%相对较高，28%/25%较低，优先与45%相邻，次选42%，避免28%"
```

---

## 关键原则总结

### 🎯 v2.1 的核心哲学

```
NO WORD LEFT BEHIND IN COMPARISON

即使在"必选TOP词"的框架下
也要考虑"次选词相对于低选词的优先级"

比较的粒度不是二元的（高/低）
而是完整的梯度链：

  秦长城90%
      ↓(靠近TOP优先)
  故宫75%  ← v2.1新增：在TOP相邻时优先靠近次级
      ↓(保留聚集空间)
  埃及20%
      ↓(避免靠近)
  泰姬陵15%
```

### ✅ 规则速记

```
1. 列出所有词的相关度百分比
2. 识别梯度特征（陡峭/平缓/平散）
3. 根据梯度选择靠近的词（TOP词原则）
4. 【NEW】在该词的相邻位置间进行"次级比较"
        ↳ 靠近"相对更高"的词
        ↳ 避免"相对更低"的词
```

---

## 测试验证

✅ **所有 25 个 aiAgent 测试通过**
- 纯 Prompt 优化，无逻辑改变
- 完全向后兼容

```
Tests passed: 25/25
Tests failed: 0/0
```

---

## 对游戏体验的深层影响

### 地图聚类的自然演化

#### 早期（Round 1-5）：初始聚类形成
```
v2：秦长城、故宫聚集
v2.1：秦长城、故宫更紧凑
      埃及、泰姬陵保持远离（被动分离）
      → 结果：更清晰的"中国文明区"
```

#### 中期（Round 6-10）：多聚类连接
```
v2：找聚类之间的"桥梁"
v2.1：桥梁连接时也优先靠近"相对更高"的词
      → 结果：多聚类间形成"强连接"而非"弱连接"
```

#### 后期（Round 11+）：整体网络成熟
```
v2：填补空白
v2.1：填补空白时也遵循相对高低
      → 结果：整个地图形成的是"有层级的网络"
             而不是"平均分布的网络"
      → 更符合历史地理的实际分布
```

---

## 从工程角度看的改进

| 指标 | v2.0 | v2.1 |
|------|------|------|
| **决策维度** | 梯度识别 | 梯度识别 + 次级比较 |
| **比较层级** | 2层（TOP vs 其他） | 3层+（梯度内部的完整排序） |
| **相对关系** | TOP的明确性 | 完整的相对高低链 |
| **聚集紧密度** | 中等 | 更紧密更有逻辑 |
| **Reasoning透明度** | 高 | 更高（包含次级对比） |
| **向后兼容** | N/A | ✅ 100% |

---

## 总结

### v2.1 添加的价值

```
从：
  看TOP词 → 靠近TOP词

到：
  看TOP词 → 靠近TOP词 → 在靠近TOP词的多个位置中 → 优先靠近"次级词" → 避免靠近"低词"

这是从"单维度决策"到"多维度权衡"的升级
```

### 实际意义

✅ **更智能的地图结构**：词汇聚类不仅按"相关性"，还按"相对层级"形成
✅ **更符合现实**：历史/地理上的聚类确实有"核心"和"外围"的分别
✅ **更易于理解**：玩家看到的地图反映了词汇的真实关联度层级
✅ **更好的游戏体验**：相似词汇不仅相近，而且形成的聚集更紧凑

---

**版本**：v2.1（v2 的精细化优化）
**Commit**: e36f473
**更新时间**: 2026-02-28（同一 session）
**兼容性**: ✅ 100% 向后兼容
**测试**: ✅ 所有 25 个单元测试通过
